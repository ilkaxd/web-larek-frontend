import { EventEmitter } from "./base/events";

class LarekPresenter extends EventEmitter{
    constructor(){
        super();
    }

    // Добавить всё что делается при кликах    
}

export { LarekPresenter }